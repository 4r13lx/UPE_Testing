#ifndef FUNC_H
#define FUNC_H

/// @brief Calcula el maximo comun divisor
/// @param a, b Valores enteros que representan a=valor divisible, b=divisor
/// @return Se retorna 0 (cero) si el argumento b es 0 (cero), X en caso de que los valores sean validos para realizar la operacion aritmetica
int CalcularMCD(int a, int b);

#endif